# Render Blueprint for Synergy Sugar Static Site Deployment
services:
  - type: web
    name: synergy-sugar
    env: static
    buildCommand: cd frontend && npm install --legacy-peer-deps && npm run build
    staticPublishPath: ./frontend/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
 